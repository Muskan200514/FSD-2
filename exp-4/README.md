1.Local State (useState): Used to manage data that is only relevant to a single component or its immediate child components.

2.Context API: A built-in React feature used for global state management, helping share data across the component tree without adding external libraries.

3.Redux: A predictable state management tool designed for large and complex applications where state changes are frequent and logic needs to be shared across multiple, disconnected components.