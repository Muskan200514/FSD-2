Read-me
1. single page application - In this the entire website works inside a single HTML page and it updates dynamically without reload the whole page.

2. Content is of 2 types - 
- static :same context provided to the user (Front-End)
- Dynamic: content created once only (Backend)

3. Mpa is a mixture of static and dynamic

4. we change the source code to exectible file (The change is called the build)

5. We makethe disct from build each of one file of HTML,CSS,JAVASCRIPT

6. Till now we have made the webpage with move,contacts.

7. We give dict and not source code to the user 
